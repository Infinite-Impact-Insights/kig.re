#!/usr/bin/env bash

project_home=$(dirname $0)
cd $project_home && pwd
git reset --hard
git pull --rebase
bundle
bundle exec jekyll build
find . -type d -exec chmod 755 {} \;
find . -type f -exec chmod o+r {} \;
cd -
